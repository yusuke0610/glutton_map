# backend
backend/data/
backend/server

# web
web/node_modules/
web/dist/
web/src/types.gen.ts
# Playwright の成果物
web/playwright-report/
web/test-results/
# E2E 用の使い捨て DB
backend/e2e.db
# 実 env ファイルは管理外（.env.example だけ追跡する）
web/.env
web/.env.*
!web/.env.example

# secrets — ルートの実 env ファイルは管理外（docker-compose が自動で読む。.env.example だけ追跡）
.env
.env.*
!.env.example

# misc
.DS_Store
result
.direnv/

# local
.study/report/

# tool 生成のログ等
report/

# tsc incremental 成果物
web/*.tsbuildinfo
