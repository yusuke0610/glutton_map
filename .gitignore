# backend
backend/data/
backend/server

# tools
tools/municipalities/node_modules/

# web
web/node_modules/
web/dist/
web/src/api/types.gen.ts
# Playwright の成果物
web/playwright-report/
web/test-results/
# E2E 用の使い捨て DB
backend/e2e.db
# 実 env ファイルは管理外（.env.example だけ追跡する）
web/.env
web/.env.*
!web/.env.example
!web/.env.test

# secrets — ルートの実 env ファイルは管理外（docker-compose が自動で読む。.env.example だけ追跡）
# パスを / で始めてリポジトリルート直下だけに限定する（無指定だと web/.env.* 等にも
# マッチしてしまい、上の !web/.env.test の再許可を無効化してしまうため）。
/.env
/.env.*
!/.env.example

# misc
.DS_Store
result
.direnv/

# local
.study/report/

# tool 生成のログ等
report/

# tsc incremental 成果物
web/*.tsbuildinfo
