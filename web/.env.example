# フロントの環境変数の雛形。コピーして web/.env.local などを作る。
# Vite は VITE_ 接頭辞の変数だけをクライアントに公開し、ビルド時に焼き込む。

# API のベースURL。必須。未設定だとアプリの起動時（import時）にエラーになる
# （サイレントに localhost へフォールバックすると設定漏れに気付けないため）。
VITE_API_BASE=http://localhost:8001

# ログ出力の最小レベル（debug/info/warn/error）。未設定なら本番warn・開発debug。
VITE_LOG_LEVEL=debug
